{
  "name": "gissen-consumer-probe",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Scratch consumer app: type-checks the packed gissen tarball from outside the monorepo.",
  "scripts": {
    "probe": "vue-tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vue": "^3.5.35",
    "vue-tsc": "^3.3.6"
  }
}
